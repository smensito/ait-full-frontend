const TrainingDashboard = () => {
  return (
    <div className="trainings_container">
      {/**
       * Add new training (Coach Role)
       * List all available trainings with data -> number of players subscribed, title, day, hour. official/unofficial
       * Delete one training (Coach Role)
       * Easy access to player subscribe/unsubscribe (Coach Player) -> Btn to join training without accesing
       **/}
    </div>
  );
};

export default TrainingDashboard;
